class Coordinate:

    def __init__(self, x, y):
        self.color = x
        self.color = y

my_coordinate= Coordinate('azul', 'rojo')

Create a class `Coordinate` that accepts two arguments during instantiation, `x` and `y`. Link them to two attributes of the class with the same names.
